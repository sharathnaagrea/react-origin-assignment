//import { expect } from 'chai';
//import * as shallow from 'enzyme';
//import * as Provider from 'react-redux';
//import { mount } from 'enzyme';

//import { EmployeeList } from '../../components/employee-list.js';

//describe('employee list', () => {
    // let component;
    // let store;
    // let value;

    // beforeEach(() => {
    //     const initialState = {
    //         employees: [],
    //         error: null
    //     };
    //     store = mockStore(initialState);
    //     component = shallow(<Provider store={store}></Provider>);
    // });

    // it('should show initial values', () => {
    //     const initialState = { employees: [], error: null };

    //     expect(mapStateToProps(initialState).employees).equal([]);
    // });

//});

